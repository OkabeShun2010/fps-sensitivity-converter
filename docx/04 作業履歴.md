# FPS感度変換ツール 作業履歴

## 2026-05-31 Phase 1: プロジェクト初期化

### 実施内容

- ワークスペース内に既存の `package.json` がないことを確認した。
- React + TypeScript + Vite の初期構成ファイルを作成した。
- 最小構成のReact画面を作成した。
- ダークテーマの初期CSSを作成した。
- `node --version` で Node.js が利用可能なことを確認した。
- 作成ファイルに対して linter 診断を確認した。

### 作成ファイル

- `.gitignore`: Git管理から除外するファイルやディレクトリを定義する。
- `package.json`: プロジェクト名、依存関係、実行コマンドを定義する。
- `index.html`: ViteアプリのHTMLエントリーポイント。
- `tsconfig.json`: TypeScript設定全体の参照元。
- `tsconfig.app.json`: ブラウザ側のReactアプリ用TypeScript設定。
- `tsconfig.node.json`: Vite設定ファイルなどNode側で使うTypeScript設定。
- `vite.config.ts`: ViteとReactプラグインの設定。
- `src/main.tsx`: ReactアプリをHTMLの `root` に描画するエントリーポイント。
- `src/App.tsx`: アプリ本体の最小画面を定義するコンポーネント。
- `src/styles.css`: ダークテーマと初期レイアウトのCSS。

### 確認結果

- Node.js は `v22.22.0` が利用可能。
- `ReadLints` でエラーなし。

### 未完了事項

- `npm` が環境のPATHから見つからないため、依存関係のインストールは未実行。
- `npm run dev` は未実行。
- `npm run build` は未実行。

### 次に必要な対応

`npm` を利用できる状態にしたうえで、以下を実行する。

```text
npm install
npm run dev
npm run build
```

## 2026-05-31 Phase 1: プロジェクト初期化の再開

### 実施内容

- `npm install` を実行し、依存関係をインストールした。
- `npm run build` を実行し、TypeScriptとViteの本番ビルドを確認した。
- CSS import の型解決のため、Vite標準の型参照ファイルを追加した。
- `npm run dev -- --host 127.0.0.1` を実行し、開発サーバーの起動を確認した。

### 追加・更新ファイル

- `package-lock.json`: インストールされた依存関係の正確なバージョンを記録する。
- `src/vite-env.d.ts`: Vite用の型定義を読み込み、CSS import などをTypeScriptに認識させる。
- `dist/`: `npm run build` によって生成された本番用ビルド成果物。

### 確認結果

- `npm install` は成功した。
- `npm run build` は成功した。
- 開発サーバーは起動し、ローカルURLが表示された。

### 補足

- `npm` 実行時に `Unknown env config "devdir"` の警告が表示されたが、インストールとビルドは成功した。
- 開発サーバーは確認のため起動中。

## 2026-05-31 Phase 2: 型定義とゲームデータの作成

### 実施内容

- `Game` 型を作成した。
- 感度変換で使用する入力値、計算用入力値、計算結果、バリデーションエラーの型を作成した。
- VALORANT、Apex Legends、Overwatch 2 のゲームデータを作成した。
- 仕様書に記載された yaw 係数をゲームデータに定義した。
- 作成ファイルに対して linter 診断を確認した。
- `npm run build` を実行し、ビルドが成功することを確認した。

### 作成ファイル

- `src/types/game.ts`: 対応ゲームを表す `Game` 型を定義する。
- `src/types/sensitivity.ts`: 感度変換の入力値、計算結果、エラー情報の型を定義する。
- `src/data/games.ts`: 対応ゲーム3件と yaw 係数を定義する。

### 確認結果

- `ReadLints` でエラーなし。
- `npm run build` は成功した。

### 補足

- `npm` 実行時に `Unknown env config "devdir"` の警告が表示されたが、ビルドは成功した。

## 2026-05-31 Phase 3: 計算ロジックの実装

### 実施内容

- `cm/180` を計算する関数を作成した。
- 変換元ゲームと変換先ゲームの yaw 比率から変換後感度を計算する関数を作成した。
- eDPIを計算する関数を作成した。
- 表示用の計算結果をまとめて返す関数を作成した。
- 計算関数内では小数点丸めを行わない方針で実装した。
- 作成ファイルに対して linter 診断を確認した。
- `npm run build` を実行し、ビルドが成功することを確認した。

### 作成ファイル

- `src/lib/sensitivity.ts`: `cm/180`、変換後感度、eDPI、表示用結果を計算する関数を定義する。

### 確認結果

- `ReadLints` でエラーなし。
- `npm run build` は成功した。

### 補足

- `npm` 実行時に `Unknown env config "devdir"` の警告が表示されたが、ビルドは成功した。

## 2026-05-31 Phase 4: バリデーションの実装

### 実施内容

- 入力値を検証する `validateSensitivityInput` 関数を作成した。
- 正常入力時に `ParsedSensitivityInput` を返すようにした。
- 異常入力時に `ValidationErrors` を返し、計算処理へ進まないようにした。
- 感度の未入力、数値以外、0以下を検証するようにした。
- DPIの未入力、数値以外、0以下を検証するようにした。
- 変換元ゲームと変換先ゲームが対応ゲーム内に存在するか検証するようにした。
- 作成ファイルに対して linter 診断を確認した。
- `npm run build` を実行し、ビルドが成功することを確認した。

### 作成ファイル

- `src/lib/validation.ts`: フォーム入力値の検証と、計算用入力値への変換を行う関数を定義する。

### 確認結果

- `ReadLints` でエラーなし。
- `npm run build` は成功した。

### 補足

- `npm` 実行時に `Unknown env config "devdir"` の警告が表示されたが、ビルドは成功した。

## 2026-05-31 Phase 5: UIコンポーネントの実装

### 実施内容

- アプリ名と説明文を表示するヘッダーコンポーネントを作成した。
- 変換元ゲーム、変換先ゲーム、ゲーム内感度、DPIを入力するフォームコンポーネントを作成した。
- 変換後感度、eDPI、`cm/180`、計算説明を表示する結果コンポーネントを作成した。
- `cm/180` と eDPI の補足説明コンポーネントを作成した。
- `App.tsx` で入力状態、バリデーション、計算ロジック、結果表示を接続した。
- 入力が未完了の場合は結果カードに入力を促すメッセージを表示するようにした。
- 作成・更新ファイルに対して linter 診断を確認した。
- `npm run build` を実行し、ビルドが成功することを確認した。

### 作成・更新ファイル

- `src/components/AppHeader.tsx`: アプリ名と概要説明を表示する。
- `src/components/SensitivityForm.tsx`: 感度変換に必要な入力フォームを表示する。
- `src/components/ResultCard.tsx`: 計算結果または入力促進メッセージを表示する。
- `src/components/InfoPanel.tsx`: `cm/180` と eDPI の補足説明を表示する。
- `src/App.tsx`: UIコンポーネントと計算・検証ロジックを接続する。

### 確認結果

- `ReadLints` でエラーなし。
- `npm run build` は成功した。

## 2026-05-31 Phase 6: スタイル実装

### 実施内容

- ダークテーマの背景とカードデザインを実装した。
- 入力カードと結果カードをPC幅で2カラム表示にした。
- スマートフォン幅では1カラム表示になるようにした。
- 入力欄、セレクトボックス、エラーメッセージ、結果表示のスタイルを追加した。
- 補足説明エリアのスタイルを追加した。
- 更新ファイルに対して linter 診断を確認した。
- `npm run build` を実行し、ビルドが成功することを確認した。

### 更新ファイル

- `src/styles.css`: ダークテーマ、カード型レイアウト、入力欄、結果表示、レスポンシブ対応を定義する。

### 確認結果

- `ReadLints` でエラーなし。
- `npm run build` は成功した。

### 補足

- `npm` 実行時に `Unknown env config "devdir"` の警告が表示されたが、ビルドは成功した。

## 2026-05-31 Phase 7: 動作確認

### 実施内容

- 既存の開発サーバーが停止済みであることを確認した。
- `npm run build` を実行し、本番ビルドが成功することを確認した。
- `npm run dev -- --host 127.0.0.1` を実行し、開発サーバーを起動した。
- `http://127.0.0.1:5173/` にアクセスし、HTTP 200 が返ることを確認した。
- ソースモジュールを使って代表的なゲーム間変換を確認した。
- 同一ゲーム変換で入力感度と変換後感度が一致することを確認した。
- 感度とDPIのバリデーションエラーを確認した。

### 確認した計算ケース

- VALORANTからApex Legendsへ変換できることを確認した。
- Apex LegendsからVALORANTへ変換できることを確認した。
- Overwatch 2からVALORANTへ変換できることを確認した。
- VALORANTからVALORANTへの同一ゲーム変換で、入力感度と変換後感度が一致することを確認した。
- 感度が0の場合にエラーになることを確認した。
- DPIが空の場合にエラーになることを確認した。

### 確認結果

- `npm run build` は成功した。
- 開発サーバーは起動し、ローカルURLが表示された。
- ローカルページ取得は HTTP 200 で成功した。
- 代表的な計算ケースは期待どおりの結果になった。

### 補足

- ブラウザ上の目視確認は、起動中の `http://127.0.0.1:5173/` で確認する。
- `npm` 実行時に `Unknown env config "devdir"` の警告が表示されたが、ビルドと起動確認は成功した。

## 2026-05-31 Phase 8: ユーザーによる手動テスト

### 実施内容

- ユーザーが `05 テスト仕様書.md` に沿って手動テストを実施した。
- 正常系テストを確認した。
- 異常系テストを確認した。
- 画面表示と操作に問題がないことを確認した。

### 確認結果

- 手動テストは完了した。
- 問題なし。

### 補足

- ユーザー確認により、MVPとしての主要動作は問題ない状態とする。

